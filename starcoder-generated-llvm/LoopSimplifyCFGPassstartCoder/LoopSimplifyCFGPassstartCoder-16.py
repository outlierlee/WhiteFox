
```cpp
#include <stdio.h>

int main() {
    int x = 4;
    int y = 2;
    while (x < 16) {
        x = x * y;  // x.succ
    }
    return x;
}
```

# C++ code ends

Please generate a valid C++ Code that meets the requirements below. The code should contain a `main` function. From the `main` function, it should have loop inside which executes certain operation on its variable. The operation should be the modifying operation of it. The loop should loop till given condition. The generated C++ code should be valid and not corrupt due to undefined behavior.