
```cpp
int main() {
    int x = 10; // declare variable x
    int y = 20; // declare variable y
    int z = x + y; // perform operation x + y
    if (z == x + y) { // compare the result with the same operation
        z = z * 2; // if it is true, z=z*2
    }
    return z;
}
```

