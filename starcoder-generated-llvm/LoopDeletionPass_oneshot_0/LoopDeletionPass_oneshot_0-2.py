
```cpp
#include <iostream>
#include <random>

int main() {
    int x = rand(); // initialize x 
    int y = rand(); // initialize y

    while (x < y) {
        int z = rand(); // sample random number to avoid code repetition
        int a = rand();
        int b = rand();
    }

    return 0;
}
```

