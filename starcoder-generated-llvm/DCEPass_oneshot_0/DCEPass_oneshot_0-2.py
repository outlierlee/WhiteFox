
```cpp
#include <iostream>
#include <random>

int main() {
    int x = rand(); // just an arbitrary number
    int y = rand(); // just an arbitrary number
    int z = rand(); // just an arbitrary number

    if (false) {
        z = x + y;
    }

    return z;
}
```
