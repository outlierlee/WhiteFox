
```cpp
int main() {
    int x = 10; // declare variable x
    int y = 20; // declare variable y
    int z = 0; // declare variable z

    // A block of code where the same operation is performed multiple times
    // with the same operands
    if (x > y) { // a sample condition
        z = x + y; // an operation with x and y
    } else {
        z = x - y; // a different operation with x and y
    }

    return z;
}
```

