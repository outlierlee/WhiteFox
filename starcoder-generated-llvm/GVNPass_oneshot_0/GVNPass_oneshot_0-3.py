
```cpp
#include <vector>
#include <string>

int main() {
  int x = (1 << 18);
  int y = (1 << 18);
  int z = 0;
  if (x < y) {
    z = x + y;
  } else {
    z = x + y;
  }
  std::vector<int> vec = {0, 1, 2, 3};
  std::string str = "hello";
  x = varargs_func(a, 18, 25, vec[1], str[3]);
  return z;
}
```
