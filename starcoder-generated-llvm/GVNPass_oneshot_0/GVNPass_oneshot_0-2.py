
```cpp
#include <vector>
#include <string>

int main() {
    int x = (1 << 31);
    int y = (1 << 20);
    int z = 0;

    if (x < y) {
        z = x + y;
    } else {
        z = x + y;
    }

    return z;
}
```
