
```cpp
int main() {
    int x = 10; // declare variable x
    int y = 20; // declare variable y
    int z = 0; // declare variable z, to store the result

    // A block of code where the same operation is performed multiple times
    // with the same operands
    if (x < y) { // x less than y
        z = x + y; // adding x and y
    } else {
        z = x + y; // the same operation as above
    }
    return z;
}
```

