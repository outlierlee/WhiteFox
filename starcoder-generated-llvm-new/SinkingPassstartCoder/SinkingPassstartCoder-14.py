
```c
#include <stdio.h>

int main() {
    int a = 10; // Declare and initialize a variable
    int r = 20; // Declare and initialize another variable
    int c; // Declare a variable to store the result

    if (a > 5) { // Conditional block
        c = a * r; // Perform an operation
    } else {
        c = a + r; // Perform a different operation
    }

    printf("%d\n", c); // Use the result

    return 0;
}
```
