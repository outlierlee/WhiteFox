
```cpp
#include <stdio.h>

int main() {
    int x = 1000; // Initialize x
    int y = 15; // Initialize y
    int limit = 1500; // constant condition value

    // Apply while loop with constant condition
    while (x < limit) {
        x += y; // Update the loop variable
    }

    return x; // Returns the result
}
```

